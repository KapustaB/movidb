<p id="notice"><%= notice %></p>

<h1>Actors</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @crew_members.each do |actor| %>
      <tr>
        <td><%= link_to 'Show', actor %></td>
        <td><%= link_to 'Edit', edit_castMember_path(actor) %></td>
        <td><%= link_to 'Destroy', actor, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New CrewMember', new_castMember_path %>
